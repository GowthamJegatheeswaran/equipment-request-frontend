# Optional.
# Leave empty for local dev (Vite proxy will forward /api -> http://localhost:8080)
#
# For deployment, set to your backend base URL (no trailing slash), e.g.
# VITE_API_BASE_URL=https://api.yourdomain.com
VITE_API_BASE_URL=
