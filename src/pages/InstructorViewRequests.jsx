import ViewRequests from "./ViewRequests"

// Instructor UI uses the same backend endpoints as Student.
// Backend role is STAFF, but it still calls /api/student/requests.
export default ViewRequests
